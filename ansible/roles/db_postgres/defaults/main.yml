---
pg_version: "17"
pg_mount_dir: /mnt/pgdata
pg_data_dir: /mnt/pgdata/data
pg_data_device: /dev/xvdb
pg_cluster_name: main
pg_db_name: seedplus
pg_db_user: seedplus

# db_port comes from group_vars/all/main.yml
# pg_password comes from group_vars/all/vault.yml (vault_pg_password)
